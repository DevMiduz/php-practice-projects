# Start from the official PHP 8.3 Apache image
FROM php:8.3-apache

# Install mysqli and other common extensions
# The 'docker-php-ext-install' command is a helper script provided by the official PHP images
RUN docker-php-ext-install mysqli pdo pdo_mysql